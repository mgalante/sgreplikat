define(['knockout',
    'jquery',
    'viewmodels/proveedor/<%=module%>',
    'durandal/system',
    'repository/<%=modules%>'], function (ko, $, <%=Module%>,system, <%=modules%>) {
    return function list()
    {
        var self = this;
        self.items = ko.observableArray();
        self.activate = function()
        {
            return <%=modules%>.getAll().then(function(items){
                    self.items(items);
                }
            );
        };
    };
});